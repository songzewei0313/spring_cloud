spring:
  application:
    name: user-center
  profiles:
    active: dev
  cloud:
    nacos:
      config: #注意是配置管理config，不是discover
        server-addr: localhost:8848 #nacos 地址
        file-extension: yaml